<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Budget Planner</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="hero">
        <h1>Budget Planner</h1>
        <p>tine-ti cheltuielile sub control, simplu si clar.</p>
        <div class="d-flex gap-3 mt-4">
            <% if (user) { %>
                <a href="/budget" class="btn-dark-custom">Dashboard</a>
            <% } else { %>
                <a href="/register" class="btn-dark-custom">Fa-ti cont</a>
                <a href="/login" class="btn-outline-custom">Autentificare</a>
            <% } %>
        </div>
    </div>
</body>
</html>